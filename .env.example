# fake serves bundled data and lets any sign-in through; api talks to the backend at VITE_API_BASE_URL.
VITE_BACKEND=fake
VITE_API_BASE_URL=http://localhost:8080
# Review pages without signing in. Honoured only with VITE_BACKEND=fake; the API never skips.
VITE_SKIP_AUTH=0
