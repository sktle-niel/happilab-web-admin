# The API this admin reads and writes. Every record, figure and sign-in comes from it; nothing is bundled.
VITE_API_BASE_URL=http://localhost:8080
# Shows "Continue with Google" when set; the API must be given the same client id.
VITE_GOOGLE_CLIENT_ID=
